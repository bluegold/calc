(define values (list 1 2 3))

(do
  (print "--- higher-order flat-map ---")
  (print (flat-map (lambda (x) (list x (+ x 10))) values))
  (print (flat-map (lambda (x) x) values))
  (flat-map (lambda (x) (list (* x 2) (* x 3))) values))
