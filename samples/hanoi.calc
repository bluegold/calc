(define (hanoi n from to via)
  (if (<= n 1)
      (println (concat from " -> " to))
      (do
        (hanoi (- n 1) from via to)
        (println (concat from " -> " to))
        (hanoi (- n 1) via to from))))

(do
  (println "--- hanoi ---")
  (hanoi 3 "A" "C" "B"))
