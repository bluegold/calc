(do
  (load "time/core")
  (define t (parse-time "2026-03-15T12:34:56.123456Z"))
  (print "--- stdlib time ---")
  (print (std.time.core.iso t))
  (print (std.time.core.iso (get (std.time.core.month-range t) :begin)))
  (std.time.core.iso (get (std.time.core.month-range t) :end)))
