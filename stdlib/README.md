# Standard Library Draft

This directory contains Calc modules intended to become a standard library.

Current modules:

- `collections/list.calc`: common list helpers
- `math/stats.calc`: basic statistical helpers
- `time/core.calc`: time formatting and month-range helpers
- `json/core.calc`: JSON convenience wrappers

Each file defines functions under a dedicated namespace so names do not collide.
